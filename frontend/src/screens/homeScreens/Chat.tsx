import { SafeAreaView, Text } from "react-native"

const Chat = () => {
    return (
        <SafeAreaView>
            <Text style={{ fontSize: 30, fontWeight: "bold", color: "red", marginTop: 50 }}>Chat</Text>
        </SafeAreaView>
    )
}

export default Chat